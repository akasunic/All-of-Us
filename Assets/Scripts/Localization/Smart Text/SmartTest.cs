using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SmartTest : MonoBehaviour
{
    public int numPeople = 0;

    private void Update() {
        //if (Input.GetKeyDown(KeyCode.P))
        //    incrementPeople();
    }

    public void incrementPeople() {
        numPeople++;
    }
}
